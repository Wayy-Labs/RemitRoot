{
  "name": "remitroot-frontend",
  "private": true,
  "version": "0.1.0",
  "packageManager": "pnpm@9.12.0",
  "scripts": {
    "dev": "pnpm -r --parallel --filter ./apps/* dev",
    "build": "pnpm -r --filter ./apps/* build",
    "start": "pnpm -r --parallel --filter ./apps/* start",
    "lint": "pnpm -r --filter ./apps/* lint"
  }
}
